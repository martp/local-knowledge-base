# Local Knowledge Base Configuration

# Ollama Configuration
OLLAMA_BASE_URL=http://host.docker.internal:11434

# RAG Configuration
RAG_EMBEDDING_MODEL=nomic-embed-text
ENABLE_RAG_HYBRID_SEARCH=true
RAG_TOP_K=8
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Authentication for Crawling Protected Sites
# Extract these from browser DevTools -> Network -> Copy as cURL
CONFLUENCE_COOKIES="JSESSIONID=your_session_id; atlassian.xsrf.token=your_token"
CONFLUENCE_AUTH="Bearer your_bearer_token"
JIRA_COOKIES="your_jira_cookies"
JIRA_AUTH="your_jira_auth"

# Add your own authenticated sites here:
# CUSTOM_SITE_COOKIES="cookie1=value1; cookie2=value2"
# CUSTOM_SITE_AUTH="Bearer your_token"